# Production-only runtime dependencies.
# Excludes the evaluation harness (ragas, datasets, full langchain) to keep the
# Docker image lean. Install requirements.txt for local dev + eval runs.

fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.4
pydantic-settings==2.10.1
structlog==25.3.0
python-dotenv==1.1.0
streamlit==1.45.1
requests==2.32.5
pyyaml==6.0.2

# RAG pipeline
openai==2.24.0
chromadb==1.5.2
pypdf==6.7.5
langchain-text-splitters==1.1.1
langchain-openai==1.1.10
tenacity==8.2.3
python-multipart==0.0.20

# Hybrid retrieval + reranking
rank-bm25==0.2.2
sentence-transformers==3.4.1

# Rate limiting
slowapi==0.1.9
